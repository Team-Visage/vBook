language: ruby
rvm:
  - 2.5.1
install:
  - bundle install
  - npm install
before_script:
  - psql -c 'create database pgapp_test;' -U postgres
  - psql -c 'create database pgapp_test;' -U postgres
  - rails db:migrate
script:
  - rspec
